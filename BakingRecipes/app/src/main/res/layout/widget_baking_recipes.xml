<?xml version="1.0" encoding="utf-8"?>
<!--cooking image source: http://icons.iconarchive.com/icons/atyourservice/service-categories/512/Cooking-icon.png-->
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/widget_background_blue"
    android:padding="@dimen/widget_margin">
    <ImageView
        android:id="@+id/widget_cooking_image"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:src="@drawable/backing_recipes_cooking_icon"
        android:contentDescription="@string/widget_icon_description"
        android:layout_centerHorizontal="true"/>
    <TextView
        android:id="@+id/widget_text"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerHorizontal="true"
        android:layout_centerVertical="false"
        android:layout_below="@+id/widget_cooking_image"
        android:layout_margin="@dimen/widget_text_view_margin"
        android:background="@color/widget_background_blue"
        android:text="@string/widget_goto_home_text"
        android:textColor="@color/white"
        android:textSize="@dimen/widget_text_view_size"
        android:textStyle="bold|italic" />

</RelativeLayout>